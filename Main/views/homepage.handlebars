{{!-- Renders each post as its own card --}}
{{#each Post as |post|}}
<div class="card">
  <h5 class="card-header">{{post.title}} | {{post.User.username}}</h5>
  <div class="card-body">
    <p class="card-text">{{post.body}}</p>
  </div>
</div>
{{/each}}
